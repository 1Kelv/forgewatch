[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "forgewatch-security"
version = "0.2.0"
description = "Standalone repository security scanning with plain-English reports and human-approved remediation"
requires-python = ">=3.9"
license = { text = "Proprietary" }
dependencies = []

[project.scripts]
forgewatch = "forgewatch.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["forgewatch*"]

[tool.setuptools.package-data]
forgewatch = ["dashboard.html"]
